### Next is Adapter—useful when connecting your application to AI providers with different APIs.

The adapter does three things:
- Implements AiModel, so your service can use it.
- Holds the provider’s client.
- Translates generate(question) into chat(question, 0.7).