### Builder constructs an object step by step, using named methods to make its configuration clear.
In your AI project, a request might contain a question, a temperature setting, and a maximum output length. Instead of writing:
```java 
    new AiRequest("Explain Builder", 0.7, 500);
```