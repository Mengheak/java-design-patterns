### This pattern passes a request through a sequence of handlers. Each handler can process it, pass it to the next handler, or stop the chain.
In your AI project, let’s validate a question before sending it to the model:
```Question → Blank check → Length check → AnswerService```