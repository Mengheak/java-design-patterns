### Facade provides a simple interface to a group of cooperating objects. It gives callers one convenient entry point for a workflow.

Your project already has several steps:
```Validate the question → Prepare the prompt → Call the model → Notify observers```