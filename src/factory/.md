### A factory centralizes object creation so the calling code doesn’t need to know how each object is constructed.

Notice that creating the two models requires different steps:
- The fake model only needs a constructor call.
- The vendor model needs a client wrapped in an adapter.

The factory hides those construction details behind one method:
`AiModel create(String type)`