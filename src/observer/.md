### Observer lets one object notify multiple interested objects when an event happens, without knowing what each listener does.
In your AI project, suppose you want to:
- Print a message when an answer is generated.
- Count how many answers have been generated.
  ```AnswerService``` will announce the event. Separate observers will react to it.