### State lets an object change its behavior when its internal state changes. Each state is represented by a separate class.

In your AI project, let’s create a chat session with two states:
```Active → Closed```

An active session can answer questions. A closed session rejects them. The caller always uses the same ask() method.