### Template Method defines the steps of an algorithm in a parent class, while letting subclasses customize selected steps.

In your AI project, different answer styles can share this workflow:
```Validate question → Prepare prompt → Call model → Format answer```